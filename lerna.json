{
    "lerna": "3.13.0",
    "npmClient": "yarn",
    "version": "independent",
    "packages": ["packages/**/*"],
    "registry": "https://registry.npmjs.org/",
    "command": {
        "publish": {
            "ignoreChanges": ["*.md", "tests/*", "ignored-files"]
        }
    }
}
